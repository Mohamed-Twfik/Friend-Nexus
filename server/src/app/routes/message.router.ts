import express from "express";

const router = express.Router();

router.get("/list/:chatId", );
router.post("/new/:chatId", );
router.patch("/:messageId", );
router.delete("/:messageId", );

export default router;